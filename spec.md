# 레디커리어 AI — 박람회 파일럿 빌드 스펙 (Antigravity 핸드오프용 단일 소스)

## 1. 제품 한 줄 정의
학생이 **진로활동을 누적 관리하며 진로를 구체화**해가는 게임화 웹앱. 흥미유형 검사로 시작해 → 관심 직업 설정 → AI 로드맵/추천활동 → 습관·목표 관리 → 진로 포트폴리오 누적.
- AI 어시스턴트 캐릭터 이름: **아리(Ari)** — 강아지 마스코트, 앱 전반의 안내자.

## 2. 파일럿 목표 (박람회 데모 기준)
1. 방문자(학생 역할)가 **로그인 → 검사 → 홈 → 4개 탭**을 끊김 없이 체험할 수 있을 것.
2. AI 기능(로드맵 생성, 활동 추천, 습관 설계, 포트폴리오 정리)이 실제로 동작하는 것을 최소 1개 이상 시연.
3. 교사(학교관리자) 화면에서 학생 진도율 조회 + 생기부 가이드안 추출이 데모 가능할 것.

## 3. 기술 스택 (확정)
- **UI/클라이언트**: Vite + React + TypeScript + Tailwind CSS (Google Stitch 디자인 DNA 계승)
- **백엔드/인증**: Supabase (Auth, PostgreSQL, RLS)
- **AI 호출**: Vercel Serverless Function 경유 (클라이언트 API Key 노출 금지)

## 4. 사용자 역할 & 권한 (3계층)
1. **슈퍼관리자 (최종마스터)**: 전체 접근. 초대코드 발급, 학급/계정 생성·관리, 가입 기간 개폐.
2. **학교관리자**: 자기 학교 학생만 조회 + 생기부 가이드안 추출 (RLS 제어).
3. **학생**: 본인 데이터만 등록/수정.

## 5. 흥미유형 검사 알고리즘 (Holland RIASEC)
- R(현실), I(탐구), A(예술), S(사회), E(진취), C(관습) 6유형 강제선택 양자택일 구조.
- 문항 및 직업 매핑은 `src/data/assessment_config.json`에 외부화.
